<?php

$hostname="localhost";
$username="root";
$password="";
$dbName="southparku";

 $conn= mysqli_connect($hostname, $username, $password, $dbName);
if (!$conn) {
     die("Connection failed: " . mysqli_connect_error());
}
